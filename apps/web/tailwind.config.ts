import type { Config } from 'tailwindcss';

/**
 * Токены оформления — фирменный стиль RenovateAM (brand/README.md).
 *
 * Имена шкал сохранены от прежней темы (`ink`, `accent`, `amber`, `success`,
 * `danger`), а значения заменены на фирменные: так утилитарные экраны —
 * кабинет и админка — получают новую палитру без переписывания разметки,
 * а семантика статусов остаётся отделена от акцента.
 *
 *  · `ink`    — нейтраль с изумрудным уклоном: фоны, границы, текст;
 *  · `accent` — основной тёмный изумруд `#0E2B25` (кнопки, ссылки, фокус);
 *  · `gold`   — шампань `#C9B183`. Только акцент: тонкие линии, знаки,
 *               кнопка на тёмном. Крупными плоскостями не заливается;
 *  · `amber` / `success` / `danger` — семантика статусов, к акценту
 *               отношения не имеет.
 */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        ink: {
          50: '#f3f6f4', // светлый фон страницы
          100: '#edf1ee', // светлая подложка секции
          200: '#dbe2de', // граница
          300: '#c2cdc7',
          400: '#93a29c',
          // Приглушённый текст. Затемнён против исходного `#6b7c76`: тот давал
          // 4.40:1 на белом и 3.86:1 на подложке `ink-100` — ниже порога 4.5:1
          // для обычного текста. Здесь 5.23:1 на белом и 4.59:1 на `ink-100`.
          500: '#5f706a',
          600: '#5a6b65', // текст приглушённый
          700: '#3a4a44',
          800: '#10201c', // текст основной
          900: '#0a1f1a', // глубокий фон (герой, футер)
        },
        accent: {
          50: '#e7eeea',
          100: '#cfddd7',
          200: '#a9c0b7',
          300: '#6e8d82',
          400: '#3d6459',
          500: '#0e2b25', // основной тёмный (изумруд)
          600: '#0c241f',
          700: '#0a1f1a',
          800: '#081916',
          900: '#06110f',
        },
        gold: {
          50: '#faf6ee',
          100: '#f0e6d2',
          200: '#e3d2b2',
          300: '#d6bf93',
          500: '#c9b183', // акцент (шампань)
          600: '#b39a6a',
          // Тёмная ступень шампани — единственная, которой красится текст
          // (цифры этапов, знаки «+/−» в вопросах). Затемнена против исходного
          // `#8f7a50`: тот давал 4.14:1 на белом. Здесь 5.33:1 на белом
          // и 4.68:1 на подложке `ink-100`.
          700: '#7d6845',
        },
        amber: {
          50: '#f7f1e3',
          100: '#eadcbd',
          500: '#b08a3e', // требует ручного расчёта
          600: '#94722f',
          700: '#6e5522',
        },
        success: {
          50: '#e8f1ec',
          100: '#c6ded2',
          500: '#2e6f52', // успех / принято
          600: '#245943',
        },
        danger: {
          50: '#f7eceb',
          100: '#e9ccc9',
          500: '#a33a32', // отклонено
          600: '#872e27',
        },
      },
      fontFamily: {
        // Интерфейс, формы, цифры.
        sans: ['var(--font-ui)', 'system-ui', 'sans-serif'],
        // Маркетинговый слой: та же гарнитура, что в логотипе.
        display: ['var(--font-display)', 'Georgia', 'serif'],
        mono: ['"Noto Sans Mono"', 'ui-monospace', 'monospace'],
      },
      // Фирменный стиль строгий: скруглений почти нет.
      borderRadius: {
        none: '0px',
        sm: '1px',
        DEFAULT: '2px',
        md: '2px',
        lg: '3px',
      },
      boxShadow: {
        // Тени только функциональные и очень мягкие, без ореолов.
        card: '0 1px 2px rgba(10, 31, 26, 0.04)',
        lift: '0 6px 18px -10px rgba(10, 31, 26, 0.28)',
      },
      maxWidth: {
        prose: '68ch',
      },
      letterSpacing: {
        brand: '0.13em',
      },
    },
  },
  plugins: [],
} satisfies Config;
